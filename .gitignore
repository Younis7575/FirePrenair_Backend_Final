venv/
*db.sqlite3
*__pycache__/
# Migrations ARE tracked. They describe the database, they are reviewed like
# code, and the server must receive them the same way it receives everything
# else. Ignoring them is why the live database and the code drifted apart.
*node_modules/
.env
*myvir
myvir
.vscode
sub_data.csv
# Firebase service-account key. Loaded by path at runtime
# (settings.py: BASE_DIR / 'fcm_service_account.json'), so it belongs on
# the server next to .env -- never in the repository.
fcm_service_account.json
